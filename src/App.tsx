import { Map } from './components/Map';

function App() {
  // The main App component is kept clean and simple.
  // It's responsible for rendering the primary components of the application.
  // For now, that's just the Map.
  return (
    <main>
      <Map />
    </main>
  );
}

export default App;